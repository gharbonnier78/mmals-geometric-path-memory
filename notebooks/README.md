# Notebooks

- `01_open_quantum_geometry.ipynb`: open-system geometry and operation-order lab.
- `02_mmals_path_memory.ipynb`: covariance-density and MMALS path-memory lab.

Run from the repository root after installing the package, or set `PYTHONPATH=src`. The GitHub Actions CI workflow executes both notebooks and publishes verified executed copies as a workflow artifact.
